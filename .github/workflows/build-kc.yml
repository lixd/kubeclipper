# 1.setup go env
# 2.build binary
# 3.upload kcctl-only artifacts

name: build-kc

on:
  push:
    branches:
      - 'master'
      - 'release*'

# This ensures that previous jobs for the branch are canceled when the branch is updated.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build and upload
    runs-on: ubuntu-latest
    env:
      GO111MODULE: on
    steps:
      - name: Check out code into the Go module directory
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Setup Golang with cache
        uses: magnetikonline/action-golang-cache@v5
        with:
          go-version-file: go.mod
      - name: Get tag or branch
        id: extract_branch
        shell: bash
        run: |
          latest_tag=$(git describe --tags --abbrev=0 2>/dev/null)
          branch=${GITHUB_REF#refs/heads/}
          if [[ "$branch" == "master" || "$branch" == "main" ]]; then
            ref_name=$branch
          elif [[ -n "$latest_tag" ]]; then
            ref_name=$latest_tag
          else
            ref_name=$branch
          fi
          echo "The ref name is $ref_name"
          echo "##[set-output name=branch;]${ref_name}"

      - name: Build
        if: github.event_name == 'push'
        run:
          KUBE_BUILD_PLATFORMS="linux/amd64 linux/arm64 darwin/amd64 darwin/arm64" make build

      - name: Package kcctl-only artifacts
        run: |
          pushd dist
          cp kcctl kcctl-linux-amd64
          cp linux_arm64/kcctl kcctl-linux-arm64
          cp darwin_amd64/kcctl kcctl-darwin-amd64
          cp darwin_arm64/kcctl kcctl-darwin-arm64
          sha256sum kcctl-* > kcctl-checksums.txt
          popd

      - name: Setup ossutil
        uses: manyuanrong/setup-ossutil@v3.0
        with:
          endpoint:  ${{ secrets.OSS_ENDPOINT }}
          access-key-id: ${{ secrets.OSS_ACCESS_KEY }}
          access-key-secret: ${{ secrets.OSS_ACCESS_SECRET }}
          #sts-token: "sts_token"
      - name: Upload to oss
        run: |
          ossutil cp -rf dist/kcctl-linux-arm64 oss://${{ secrets.OSS_BUCKET }}/kc/${{ steps.extract_branch.outputs.branch }}/
          ossutil cp -rf dist/kcctl-linux-amd64 oss://${{ secrets.OSS_BUCKET }}/kc/${{ steps.extract_branch.outputs.branch }}/
          ossutil cp -rf dist/kcctl-darwin-amd64 oss://${{ secrets.OSS_BUCKET }}/kc/${{ steps.extract_branch.outputs.branch }}/
          ossutil cp -rf dist/kcctl-darwin-arm64 oss://${{ secrets.OSS_BUCKET }}/kc/${{ steps.extract_branch.outputs.branch }}/
          ossutil cp -rf dist/kcctl-checksums.txt oss://${{ secrets.OSS_BUCKET }}/kc/${{ steps.extract_branch.outputs.branch }}/
