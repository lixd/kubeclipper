# KubeClipper OCI 交付分支 · 验收状态报告（2026-09-26 快照）

> **快照点**：分支 `feat/oci-operation-v2-migration`，HEAD `9ab302e0`（已同步 fork 与 origin 裸仓）；
> 平台 **v2.0.3-rc.25**（revision `b459b0a2`）。
> **本文用途**：一页看清"现状 / 覆盖 / 遗留"。细节证据见
> [`core-feature-checklist.md`](core-feature-checklist.md)（逐项状态）、
> [`round-2026-09-gaps.md`](round-2026-09-gaps.md)（缺口与修复台账）、
> [`R7 报告`](../superpowers/issues/2026-09-19-core-feature-e2e-r7-sh-dev-2-3-4.md)（§12.x 每轮完整证据）、
> [`acceptance/`](acceptance/)（发布验收记录）。

---

## 1. 平台与环境现状

| 项 | 现状 |
|---|---|
| 平台版本 | v2.0.3-rc.25（revision `b459b0a2`），`kcctl status` **3/3 Healthy**（kc-server/kc-etcd/kc-agent），无集群，doctor **25/25**（R27 实测） |
| 拓扑 | 3 节点：sh-dev-2 `172.16.131.208`（4c/7.9GB）、sh-dev-3 `172.16.131.146`（8c/16GB，兼共享 Registry 宿主）、sh-dev-4 `172.16.131.230`（8c/16GB） |
| 关键配置 | 登录限流 **5 次 / 10 分钟**（R24 修复零值锁死后写入）；MFA 关闭（各轮测试后还原）；registry 资源 `kc-image-registry` 与 `kc-package-registry` 均为 **http**（后者 R26 更正）；管理员口令经 `--initial-password` 设置（值不入文档） |
| 共享 Registry | `172.16.131.146:5003`，本分支累计 **26 个 `v2.0.3-rc.*` tag**（R19–R28 期间新增 rc.10→rc.25（16 个）），**只增不删**；dev-3 `kc-oci-r3-registry.service` 未改 |
| 交付链 | qualification run `36216971172`（success，19m4s）；首个验收记录 [`acceptance/3be0ffe8….yaml`](acceptance/3be0ffe83ea3054a77f66bd16ca54220ad984f24.yaml)（钉板 manifest `sha256:2e5bf6be74aa…`）；`release-gate.sh` 真机 **PASS + 两类 BLOCK**（未 bump tag / 篡改记录） |
| 仓库 | 本会话 11 个代码/发布提交 + 7 个文档提交（含本报告）；工作树干净 |
| 安全不变量 | 共享 Registry 只增 tag；平台 etcd 数据目录 `/var/lib/kc-etcd` 全程未动；仓库/文档不含 kubeconfig、token、私钥、Registry 凭据、测试口令；测试用户/集群/临时脚本全清 |

## 2. 验收方法（证据链）

1. **逐项清单**：`core-feature-checklist.md` 194 行 case，每行状态 + 轮次 + 证据出处（章节/§号/Operation ID/命令）。
2. **真机矩阵**：全部结论来自 3 节点真实平台（部署、建群、升级、增删、故障注入、API/CLI 正负向），CI/单测只作为部分验证。
3. **发布门禁**：qualification workflow（制品 digest 校验）→ 验收记录（钉板 manifest sha256）→ `release-gate.sh`（版本/sourceRevision/记录三方一致性），PASS 与 BLOCK 场景均真机验证。
4. **每轮台账**：R7 报告 §12.15–§12.25 逐轮记录命令、Operation ID、日志、终态与清理。

## 3. 测试覆盖情况

### 3.1 总表（194 行）

| 章节 | ✅ | ⚠️ | ❌ | 小计 | 说明 |
|---|---:|---:|---:|---:|---|
| 1 平台本身部署 | 28 | 1 | 0 | 29 | 仅剩 ghcr 直装（待真实发布） |
| 2 集群相关操作 | 76 | 1 | 1 | 78 | 建群/升级/扩缩/增删/证书/CIDR/CA/断网等主路径全绿 |
| 3 `kcctl` 命令 | 27 | 1 | 0 | 28 | 每条命令核心路径 + 典型失败路径 |
| 4 其他核心平台功能 | 16 | 0 | 1 | 17 | 备份/审计/IAM/登录/敏感信息全绿；剩 console E2E |
| **5 Operation V2 可靠性** | **14** | **0** | **0** | **14** | **全绿**（本轮程序重点） |
| 6 发布工程与门禁 | 7 | 6 | 0 | 13 | 机制闭环，多数待真实发布轮 |
| 7 扩展能力与环境 | 4 | 0 | 11 | 15 | 环境/外部依赖为主，不计核心通过率 |
| **合计** | **172（88.7%）** | 9 | 13 | **194** | 无发布阻断项 |

### 3.2 各章覆盖要点（摘要）

- **第 1 章**：在线/离线部署、deploy 幂等与失败路径、三节点 HA、HA 故障窗口（停 server/console 上游）、部署侧断连、纯离线 bundle（export→断网部署→建群→升级→删除）、join 负向与安全边界。
- **第 2 章**：建群正/负向（CIDR 冲突 8 项矩阵、主机网段冲突、缺省 imageRegistry 400）、1M/1M1W/master+worker、自带 CA、真实断网建群、feature-gates 与 preflight ignore 透传（真机取证到 static pod 参数）、滚动升级 1.35.8→1.37.0、升级故障注入与 retry（不可变 plan 哈希）、master add/remove（etcd quorum/VIP 收敛）、节点 disable/转 region/失联收敛/注销重建、备份与 Cron、**升级 drain PDB 死锁修复**。
- **第 3 章**：get/set/create/delete/upgrade/operation/registry/resource/delivery-policy/join/drain/login/doctor/completion 全覆盖；典型失败路径（重复 join、used 节点 drain、非法策略、非法 deploy-config、无 TTY 确认）逐条验证。
- **第 4 章**：nfs-csi/metallb 安装卸载与失败清理、operation logs 与失败原因、审计事件、**IAM 全矩阵**（用户/角色 CRUD、enable/disable、改密、登录记录、RBAC 403）、**口令策略**（创建/改密强制）、**登录限流**（阈值/隔离/窗口/清零）、**MFA 验证码全流程**（428→发码→校验、错码/复用/过期/重发限流）、token 生命周期与审计脱敏。
- **第 5 章（全绿）**：Operation/Task/ExecutionLock 可追踪、Server/Agent 重启恢复、Watch 410 relist、多节点 barrier 与输出传递、自动+人工 retry、同集群互斥（含"Updating 拒绝删除"+锁串行）、cancel 自动收敛与幽灵锁驱逐、超时语义（操作期限 + **step 级 timeout**）、日志 offset 不重复不串流、重复提交去重、fail-closed（invalidExecutionFacts）、终态写入/响应丢失不重放。
- **第 6 章**：策略一致性（CI 双处 `release-policy-verify`）、制品完整性（候选 manifest **110 制品**：package-image 10 + helm-chart 2 + runtime-image 98）、provenance（revision 双拦截 + 门禁）、制品不可变性（发布器拒绝重指 tag，两次实证）、qualification 发布链、legacy 入口静态门禁。
- **第 7 章**：`kcctl completion`、**并发容量基线**（2×1M 并发建群 ~4.6min，平台全程 Healthy）、feature-gates/preflight ignore（真机）。

### 3.3 本轮程序（R19–R28）解决的问题

| 轮次 | 候选 | 关键修复/闭环 |
|---|---|---|
| R19 | rc.10 | Docker CRI 废弃入口清理（CLI/服务端/代码全删）、console/kcctl 升级（step 2）、HA 故障窗口验收 |
| R20 | rc.11 | 升级故障注入矩阵（4 场景）、`--version` 在线正向下载（镜像法） |
| R21 | rc.11 | checklist ⚠️/❌ 集中收口 14 项 + 新发现 8 项 |
| R22 | rc.12-15 | 8 项新发现修复：幽灵锁驱逐、已删集群操作泄漏、1M taint 死锁、join sudo 循环、非 root 中转、agent 跨节点读、`--initial-password` 失效、authentication 段 panic |
| R23 | rc.16 | 升级 drain PDB 死锁（`--timeout=120s`）；**首个验收记录 + release-gate 真机**；顶层 initialPassword 拒绝、arm64 误发守卫 |
| R24 | rc.17-20 | **6 项**：限流零值配置致 admin 永久锁死（实测复现+双层修复）、登录记录未落库、角色 API 500 与悬挂绑定、MFA 四联缺陷（未注册 crash-loop/验证码 panic/键冲突/过期 token 不清理）、**审计明文记录口令与 token** |
| R24-B | rc.21 | `--feature-gates` 建群必败（kubeadm v1beta4 渲染位置）→ 组件 extraArgs + kubelet，真机取证 |
| R25 | rc.22 | **step 级 timeout 从未执行**（步骤挂 90min、cancel 失效的根因）→ 逐 step 超时生效 |
| R26 | rc.23-24 | registry 协议探测（建群拉镜像失败闭环）、状态码 3 处（400/404/503/429）、CLI 无 TTY 崩栈与 `-y`、文案 |
| R27 | — | 5-09 以代码守卫+定向单测闭环；3-15 drain→重复→重新纳管完整生命周期；gaps 文档去重（2252→315 行）+5 行证据回填 |
| R28 | rc.25 | **口令策略落地**（用户创建/改密强制 8-16 位含大小写数字）、6-13 旧入口现场核查、4-04 定性、7-03 容量基线 |

## 4. 遗留问题（22 行 + 记录项，均无发布阻断）

### 4.1 待真实 stable 发布轮（机制已闭环，只差执行）

| Case | 内容 | 前置动作 |
|---|---|---|
| 6-04 | provenance 完整闭环 | bump `packaging/resources.yaml` release→v2.0.3 → 重跑 qualification → 新验收记录 → 打 tag → release workflow |
| 6-08 | qualification 产物须能完成真实部署+建群 | 同上（发布后从产物部署一次） |
| 1.1-01 | 默认 ghcr 直装 | 同上（从 ghcr 直装而非先 sync） |

> 这是对**外可见**动作（打 stable tag + 推送 ghcr 包），需明确授权后执行。

### 4.2 无环境 / 依赖外部（当前做不了）

- **4-07 console UI E2E**：fork console 分支未配镜（需 npm 镜像环境构建 console 产物）。
- **6-13 Console 侧**：部署的 `/etc/kc-console/dist` 两个 bundle 仍含 `nfs-provisioner`（存储 addon 选项 + 安装表单定义）——需改 console 源码分支；平台侧（进程/unit/端口/策略/包清单）已核查全清。
- **6-06 / 6-10 arm64**：无 arm64 真机（用户已确认理论无碍）。
- **6-11 Tier 1 OS 矩阵**：需先定正式支持清单与机器。
- **2.1-32 IPv6 双栈**：三机无可控 IPv6 环境。
- **4-03 metallb BGP**：需邻居 AS 环境。
- **2.1-15 跳过 CNI**：需第三方 CNI 制品/环境完成"装第三方 CNI 后恢复 Ready"。
- **第 7 章扩展 7 项**：4-09 templates、4-10 DNS domains、4-11 cloudproviders、4-12a Web 终端、4-12b Pod exec、4-15 PlatformSetting、7-01 第三方 OAuth（API 路由已在代码中，属"实现但未验收"的扩展能力）。

### 4.3 已定性为产品缺口（非待办，发布若承诺需先实现）

| Case | 定性 |
|---|---|
| 2.2-04 | convertNodes：API 只有 add/remove 两类操作，无独立 master↔worker 转换入口 |
| 2.6-11 | bootstrap/standalone extension 与集群 packagePlan 共槽（node 操作内嵌步骤） |
| 4-04 | Addon 同组件多实例：API 按组件去重，第二次安装即使 scName 不同也 400 |
| 3-01 | `kcctl deploy` 纯离线未通过（在线/同步仓库/离线三态中离线缺一步） |

### 4.4 已文档化的小瑕疵（记录项，未改行为）

- `POST /oauth/logout` 需 JSON Content-Type/Accept（否则 406，go-restful 协商，客户端行为）；
- 第 5 次错口令返回 401 + reason（其后 429，含窗口时长）；
- tokens API 无创建/删除路由（`CreateTokens` handler 未注册）；
- scheme `AutomaticRetry` 字段未被消费（重试只由 `RetryTimes` 驱动）；
- 5-09 真机网络注入未做（"写入落地仅响应丢失"的时序，已由定向单测覆盖）；
- `kcctl delete cluster -F` 强制删在用节点未单独跑（R18 master remove 已覆盖同族强制路径）。

## 5. 复现与再验证指引

```bash
# 平台健康（在 server 节点，如 sh-dev-2）
kcctl status && kcctl doctor            # 期望 3/3 Healthy、25/25

# 发布门禁（本地，需 qualification manifest 与验收记录）
scripts/open-packaging/release-gate.sh \
  --release-tag v2.0.0 \
  --candidate-sha 3be0ffe83ea3054a77f66bd16ca54220ad984f24 \
  --qualification-manifest <release-manifest.yaml>   # 期望 PASS；改 tag/改记录 期望 BLOCK

# 冒烟（任一集群场景，参考 R7 报告 §12.x 的命令原文）
kcctl create cluster --name smoke --master <ip> --k8s-version v1.37.0 \
  --image-registry kc-package-registry --untaint-master
```

- 逐项状态与证据出处：`docs/testing/core-feature-checklist.md`
- 缺口/修复台账：`docs/testing/round-2026-09-gaps.md`（P0 行 1–17 全闭环；P1 仅行 8 console 待环境）
- 每轮完整证据：R7 报告 §12.15–§12.25

## 6. 环境边界与安全约束（持续生效）

- 共享 Registry `172.16.131.146:5003` 与 dev-3 `kc-oci-r3-registry.service`：**只增 tag**，不删除/不修改；其他团队物料（caas4/* 等）不动。
- 平台 etcd 数据目录 `/var/lib/kc-etcd` 未动（集群侧 `/var/lib/etcd` 可清）。
- 仓库与文档不含凭据（kubeconfig/token/私钥/Registry 口令/测试口令）；临时凭据文件即用即删。
- arm64 无环境不验证（用户确认理论无碍）；测试集群/用户/脚本每轮清理，平台保持 Healthy 终态。
